#!/bin/bash
# Run commitlint validation
npx --no -- commitlint --edit $1

# If validation passes, save the commit message for future use
if [ $? -eq 0 ]; then
  cp "$1" COMMIT_MESSAGE.txt
  echo "💾 Commit message saved to COMMIT_MESSAGE.txt for future reference"
fi